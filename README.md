# gym-sql-analysis
A SQL project analyzing gym members data with data cleaning, normalization into multiple tables (members, workouts, sessions), and extracting insights using aggregations, subqueries, and relational queries.
